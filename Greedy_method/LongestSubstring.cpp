#include <iostream>
using namespace std;

int main()
{
    // in dynamic programming
    // every possible solution is choosen
    // the optimized one among them is choosen

    // the difference between brute force and dp is that it stores the pre-calculated result and uses it as and when needed

    return 0;
}